# 测试环境端口
HOST_PORT=18082

# AI 配置（测试环境可复用同一 key）
AI_API_KEY=your-deepseek-api-key
AI_ENDPOINT=https://api.deepseek.com/v1/chat/completions
AI_MODEL=deepseek-chat

# JWT 密钥（测试环境建议使用不同的密钥）
JWT_SECRET=your-jwt-test-secret
